<div class="card">
  <% @articles.each do |article| %>
  <div class="card-body">
    <h2 class="card-title" ><%= link_to article.title, article_path(article) %></h2>
    <p class="card-text"> <%= article.content %> </p>
  </div>
  <% end %>
</div>
